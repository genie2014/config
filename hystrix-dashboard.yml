server:
  port: 9002

eureka:
  client:
    healthcheck:
      enable: true

spring:
  security:
    user:
      name: admin
      password: admin

turbine:
  appConfig: eureka-client,service-one
  clusterNameExpression: "'default'"
