server:
  port: 8001

eureka:
  client:
    healthcheck:
      enabled: true

feign:
  client:
    config:
      default:
        loggerLevel: basic
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: info,health,hystrix.stream
